# Anchor 0.30.1 requiere stable >=1.80.
# bpfel-unknown-unknown NO se instala via rustup en stable —
# lo gestiona el toolchain de Solana/Anchor automáticamente.
[toolchain]
channel    = "1.81.0"
components = ["rustfmt", "clippy"]
